# 文档模板填充功能提取项目计划

## 目标
从参考项目 docassemble 中提炼文档模板填充能力，构建一个前后端分离的 Web 应用，实现：
- 前端：React + TypeScript + Tailwind（已初始化），提供模板选择、字段填写、输出格式选择与结果下载体验。
- 后端：Python 技术栈（推荐 FastAPI）提供模板解析、数据填充、格式转换 API；服务需独立于 docassemble 运行。
- 支持模板类型 `.docx`、`.pdf` 等，输出涵盖 docassemble 现有能力（DOCX、PDF、HTML、RTF、TeX、Markdown 等）。

## docassemble 现有能力
- 模板类型：`docx_template_file`、`pdf_template_file`、`rtf_template_file` 等。
- 数据填充：
  - DOCX：`docxtpl` 渲染 Jinja2 模板，支持图片、子模板、条件逻辑。
  - PDF：基于 `AcroForm` 写入文本、复选框、签名图像；可配置导出值。
- 格式输出：
  - DOCX 模板可输出 `docx`、`pdf`、`rtf`、`rtf to docx`、`tex`、`html`、`md` 等。
  - PDF 模板输出 `pdf`，可选扁平化、PDF/A、加密。
  - 手动附件可输出任意扩展名文件。
- 高级特性：PDF/A 转换、扁平化、密码保护、DOCX 引用更新、超链接样式、子模板合并、图像嵌入等。

## 需求范围
### 前端（React + TS + Tailwind）
- **模板选择**：
  - 内置 DOCX 模板列表供用户选择（显示示例名称、预览/说明）。
  - 后续可扩展自定义上传。
- **字段填写**：
  - 根据模板字段定义生成表单（文本、数字、布尔、日期、文件上传等）。
  - 提供 JSON 查看/编辑模式（可选）。
- **输出格式选择**：
  - 列出后端支持的格式（DOCX、PDF、HTML、RTF、TeX、Markdown 等），允许多选。
  - 展示高级选项（如 PDF/A、扁平化、加密等）。
- **生成与下载**：
  - 触发生成任务，展示进度/状态。
  - 生成完成后提供下载按钮，可一次下载多种格式（组合打包或单个）。
  - 提供错误提示与重试机制。

### 后端（Python, 推荐 FastAPI）
- 接收模板标识与字段 JSON，调度 DOCX/PDF 填充流程。
- 支持格式转换链，输出 Docassemble 覆盖的各类格式。
- 管理任务状态、临时文件、日志；提供结果下载接口。
- 处理高级选项（PDF/A、扁平化、密码、附加文档等）。

## 技术路径
- **前端实现**：
  - 组件：模板库、字段表单、输出格式选择器、任务状态展示、下载列表。
  - 状态管理：本地状态或 Zustand/Redux（视复杂度），处理表单、任务结果、错误状态。
  - API 对接：使用 Axios/fetch 调用后端；处理文件上传（多部分）、下载（Blob）。
- **后端实现**：
  - 框架：FastAPI + Pydantic 数据模型，提供 Swagger/OpenAPI 文档。
  - 核心库：`docxtpl`、`python-docx`、`docxcompose`、`pikepdf`、`xfdfgen`、`pdftk`、`qpdf`、ImageMagick、Pandoc、LibreOffice（视功能）。
  - 模板管理：内置模板存储在后端（文件系统/数据库），前端获取模板列表和字段定义。
  - 转换流程：封装 Pandoc 调用产出多格式；必要时写转换适配层处理 docx→pdf→html 等链路。
  - 文件处理：使用临时目录存放中间文件，生成后返回下载链接；可扩展对象存储。
  - 异步任务：FastAPI BackgroundTasks 或 Celery + Redis 处理耗时任务。

## API 设计初稿
- `GET /api/templates`：返回可用模板列表及字段定义。
- `POST /api/templates/render`：提交模板 ID、字段 JSON、输出格式，返回任务 ID。
- `GET /api/templates/{task_id}`：查询任务状态、获取结果文件列表。
- `GET /api/templates/{task_id}/files/{format}`：下载指定格式结果。
- `GET /api/formats`：列出支持的输出格式与高级选项。

## 里程碑
1. **分析阶段（M1）**
   - 完整梳理 docassemble 模板/格式能力与依赖。
   - 明确前端功能流程、后端 API 列表、部署架构。
2. **设计阶段（M2）**
   - 前端页面结构、组件划分、字段配置模型。
   - 后端系统设计：模块拆分、数据模型、任务执行、模板管理、配置管理。
   - 定义格式支持矩阵、接口契约、错误码、日志规范。
3. **实现阶段（M3）**
   - 后端基础搭建与核心能力实现（DOCX 渲染、PDF 填表、多格式输出）。
   - 前端页面开发：模板选择、字段填写、格式选择、生成与下载交互。
   - 前后端联调，确保上传、生成、下载流程通畅。
4. **验证阶段（M4）**
   - 单元测试、端到端测试覆盖主要流程（含多格式、异常、超时等）。
   - 多模板兼容性、性能测试、依赖验证（Pandoc、LibreOffice 等）。
   - 用户体验测试，迭代调整表单与反馈信息。
5. **交付阶段（M5）**
   - 文档输出：架构说明、部署指南、API 文档、前端操作手册、模板开发指南。
   - 打包 Docker 镜像或部署脚本。
   - 定义运维策略（日志、监控、告警、备份）。

## 风险与缓解
- **依赖与部署复杂度**：通过容器化或安装脚本统一环境；划分基础/高级功能路径降低门槛。
- **模板兼容性**：提供模板制作规范与校验工具，覆盖常见示例。
- **性能瓶颈**：大模板/复杂转换需异步处理与资源限制。
- **安全性**：文件上传需校验类型、限制大小、隔离执行；后续可加入认证授权。
- **前端字段配置**：需保证模板字段定义准确，考虑自动生成与手动调整结合。

## 后续工作
- 扩展自定义模板上传与管理、模板预览、字段映射可视化。
- 集成对象存储（S3 等）与队列服务提升扩展性。
- 设计多语言界面与本地化输出支持。
- 调研或集成替代库（`borb`, `pdfplumber`, `docx2pdf` 等）优化依赖与性能。
